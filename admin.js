const adminState = {   appointments: [],   payments: [],   services: [],   settings: null };  const demoKey = 'mgAestheticSpaDemoStore'; const demoSeed = {   services: [     {       id: 'deep-tissue-massage',       name: 'Deep Tissue Massage',       description: 'Focused pressure massage for deep muscle tension and body recovery.',       durationMinutes: 60,       price: 500,       active: true     },     {       id: 'swedish-massage',       name: 'Swedish Massage',       description: 'Classic full-body massage for circulation, relaxation, and stress relief.',       durationMinutes: 60,       price: 400,       active: true     },     {       id: 'aromatherapy-massage',       name: 'Aromatherapy Massage',       description: 'Massage therapy paired with calming aromatic oils for a soothing experience.',       durationMinutes: 60,       price: 450,       active: true     },     {       id: 'prostate-massage',       name: 'Prostate Massage',       description: 'Specialized massage service handled with privacy and professional care.',       durationMinutes: 60,       price: 650,       active: true     },     {       id: 'relaxation-massage',       name: 'Relaxation Massage',       description: 'Gentle massage designed to calm the body, ease stress, and restore balance.',       durationMinutes: 60,       price: 400,       active: true     },     {       id: 'back-pain-relief-massage',       name: 'Back Pain Relief Massage',       description: 'Targeted back therapy for stiffness, soreness, and everyday body pain.',       durationMinutes: 60,       price: 650,       active: true     },     {       id: 'erotic-massage',       name: 'Erotic Massage',       description: 'Private specialty massage appointment with discreet booking handling.',       durationMinutes: 60,       price: 700,       active: true     },     {       id: 'nuru-massage',       name: 'Nuru Massage',       description: 'Premium specialty massage session with elevated privacy and preparation.',       durationMinutes: 75,       price: 1000,       active: true     },     {       id: 'body-scrubbing',       name: 'Body Scrubbing',       description: 'Full body exfoliation treatment for smoother, refreshed skin.',       durationMinutes: 45,       price: 450,       active: true     },     {       id: 'facials-acne-treatment',       name: 'Facials and Acne Treatment',       description: 'Facial care focused on acne treatment, cleansing, and skin renewal.',       durationMinutes: 60,       price: 400,       active: true     },     {       id: 'razor-bumps-treatment',       name: 'Razor Bumps Treatment',       description: 'Targeted treatment for razor bumps and irritated skin.',       durationMinutes: 45,       price: 300,       active: true     },     {       id: 'teeth-whitening',       name: 'Teeth Whitening',       description: 'Single-session teeth whitening treatment.',       durationMinutes: 30,       price: 100,       active: true     }],   appointments: [],   payments: [],   settings: {     depositPercentage: 40,     currency: 'GHS',     cancellationPolicy: 'Deposits become non-refundable after appointment confirmation. Reschedules require at least 24 hours notice.',     reminderHoursBefore: 24,     availableTimes: ['09:00', '10:30', '12:00', '13:30', '15:00', '16:30']   } };  const money = value => `${adminState.settings?.currency || 'GHS'} ${Number(value || 0).toLocaleString()}`;  async function api(path, options) {   try {     const response = await fetch(path, {       headers: { 'Content-Type': 'application/json' },       ...options     });     const data = await response.json();     if (!response.ok) throw new Error(data.error || 'Request failed');     return data;   } catch (error) {     return demoApi(path, options);   } }  function readDemoStore() {   const saved = localStorage.getItem(demoKey);   return saved ? JSON.parse(saved) : structuredClone(demoSeed); }  function writeDemoStore(data) {   localStorage.setItem(demoKey, JSON.stringify(data));   return data; }  async function demoApi(path, options = {}) {   const method = options.method || 'GET';   const data = readDemoStore();    if (method === 'GET' && path === '/api/admin/appointments') return data.appointments;   if (method === 'GET' && path === '/api/payments') return data.payments;   if (method === 'GET' && path === '/api/services') return data.services;   if (method === 'GET' && path === '/api/settings') return data.settings;    if (method === 'PATCH' && path.startsWith('/api/appointments/')) {     const id = path.split('/').pop();     const payload = JSON.parse(options.body || '{}');     const appointment = data.appointments.find(item => item.id === id);     if (!appointment) throw new Error('Appointment not found');     Object.assign(appointment, payload, { updatedAt: new Date().toISOString() });     writeDemoStore(data);     return appointment;   }    if (method === 'POST' && path === '/api/settings') {     Object.assign(data.settings, JSON.parse(options.body || '{}'));     data.settings.depositPercentage = Number(data.settings.depositPercentage);     data.settings.reminderHoursBefore = Number(data.settings.reminderHoursBefore);     writeDemoStore(data);     return data.settings;   }    throw new Error('Demo route not available'); }  function escapeHtml(value) {   return String(value ?? '').replace(/[&<>"']/g, char => ({     '&': '&amp;',     '<': '&lt;',     '>': '&gt;',     '"': '&quot;',     "'": '&#039;'   }[char])); }  function serviceImage(service) {   if (service.id.includes('teeth')) return 'assets/service-teeth.jpg';   if (service.id.includes('hot-stone')) return 'assets/service-hot-stone.jpg';   if (service.id.includes('body')) return 'assets/service-body-scrub.jpg';   if (service.id.includes('facial') || service.id.includes('acne')) return 'assets/service-facial.jpg';   if (service.id.includes('skin') || service.id.includes('razor')) {     return 'assets/service-skin-treatment.jpg';   }   return 'assets/service-massage.jpg'; }  function renderStats() {   document.querySelector('#totalBookings').textContent = adminState.appointments.length;   document.querySelector('#confirmedBookings').textContent = adminState.appointments.filter(item => item.status === 'confirmed').length;   document.querySelector('#pendingBookings').textContent = adminState.appointments.filter(item => item.status === 'pending_payment').length;    const deposits = adminState.payments     .filter(payment => payment.status === 'confirmed' || payment.status === 'pending')     .reduce((sum, payment) => sum + Number(payment.amount), 0);    document.querySelector('#depositTotal').textContent = money(deposits); }  function renderAppointments() {   const tbody = document.querySelector('#appointmentsTable');   tbody.innerHTML = adminState.appointments.map(appointment => `     <tr>       <td>         <strong>${escapeHtml(appointment.customer.fullName || 'Walk-in client')}</strong>         <small>${escapeHtml(appointment.customer.phone)}<br>${escapeHtml(appointment.customer.email)}</small>       </td>       <td>${escapeHtml(appointment.serviceName)}</td>       <td><strong>${escapeHtml(appointment.date)}</strong><small>${escapeHtml(appointment.time)}</small></td>       <td>         <strong>${money(appointment.depositAmount)}</strong>         <small>${escapeHtml(appointment.payment?.method || 'payment')} / ${escapeHtml(appointment.payment?.status || 'pending')}</small>       </td>       <td><small>${appointment.termsAcceptedAt ? new Date(appointment.termsAcceptedAt).toLocaleString() : 'Not recorded'}</small></td>       <td><span class="status-pill ${appointment.status}">${appointment.status.replace('_', ' ')}</span></td>       <td>         <div class="table-actions">           <button data-action="confirmed" data-id="${appointment.id}" title="Approve">Approve</button>           <button data-action="rescheduled" data-id="${appointment.id}" title="Reschedule">Move</button>           <button data-action="cancelled" data-id="${appointment.id}" title="Cancel">Cancel</button>         </div>       </td>     </tr>   `).join('') || '<tr><td colspan="7">No bookings yet.</td></tr>'; }  function renderPayments() {   const feed = document.querySelector('#paymentFeed');   feed.innerHTML = adminState.payments.slice().reverse().map(payment => {     const appointment = adminState.appointments.find(item => item.id === payment.appointmentId) || {};     return `       <div class="feed-item">         <span>${escapeHtml(payment.status)}</span>         <strong>${money(payment.amount)}</strong>         <small>${escapeHtml(payment.provider)}<br>${escapeHtml(appointment.serviceName || payment.appointmentId)}</small>       </div>     `;   }).join('') || '<p class="form-message">No payment records yet.</p>'; }  function renderServices() {   const list = document.querySelector('#serviceList');   list.innerHTML = adminState.services.filter(service => service.active && service.bookable !== false && Number.isFinite(Number(service.price))).map(service => `     <div class="service-row">       <div class="service-row-main">         <img src="${serviceImage(service)}" alt="">         <div>           <strong>${escapeHtml(service.name)}</strong>           <small>${escapeHtml(service.durationMinutes)} min / ${escapeHtml(service.bookable === false ? 'call to book' : 'online booking')}</small>         </div>       </div>       <span>${Number.isFinite(Number(service.price)) ? money(service.price) : 'Call to book'}</span>     </div>   `).join(''); }  function renderSettings() {   const form = document.querySelector('#settingsForm');   form.depositPercentage.value = adminState.settings.depositPercentage;   form.reminderHoursBefore.value = adminState.settings.reminderHoursBefore;   form.cancellationPolicy.value = adminState.settings.cancellationPolicy; }  async function refresh() {   const [appointments, payments, services, settings] = await Promise.all([     api('/api/admin/appointments'),     api('/api/payments'),     api('/api/services'),     api('/api/settings')   ]);    adminState.appointments = appointments;   adminState.payments = payments;   adminState.services = services;   adminState.settings = settings;    renderStats();   renderAppointments();   renderPayments();   renderServices();   renderSettings(); }  async function changeStatus(event) {   const button = event.target.closest('button[data-action]');   if (!button) return;    const payload = { status: button.dataset.action };    if (button.dataset.action === 'rescheduled') {     const date = window.prompt('New date, format YYYY-MM-DD');     const time = window.prompt('New time, format HH:MM');     if (!date || !time) return;     payload.date = date;     payload.time = time;   }    await api(`/api/appointments/${button.dataset.id}`, {     method: 'PATCH',     body: JSON.stringify(payload)   });    await refresh(); }  async function saveSettings(event) {   event.preventDefault();   const form = event.currentTarget;   const message = document.querySelector('#settingsMessage');   const payload = Object.fromEntries(new FormData(form).entries());    try {     await api('/api/settings', {       method: 'POST',       body: JSON.stringify(payload)     });     message.textContent = 'Policy settings saved.';     await refresh();   } catch (error) {     message.textContent = error.message;   } }  document.querySelector('#appointmentsTable').addEventListener('click', changeStatus); document.querySelector('#settingsForm').addEventListener('submit', saveSettings); refresh().catch(error => {   document.body.insertAdjacentHTML('afterbegin', `<p class="form-message">${error.message}</p>`); });